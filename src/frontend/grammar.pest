WHITESPACE = _{ " " | "\t" | NEWLINE }
COMMENT = _{ ("//" ~ (!NEWLINE ~ ANY)* ~ NEWLINE) | "/*" ~ (!"*/" ~ ANY)* ~ "*/" }
sep = _{ "_" | "-"}
identifier = @{ ASCII_ALPHA+ ~ (sep | ASCII_ALPHA | ASCII_DIGIT)* }
number = @{ ASCII_DIGIT+ }
expr = { identifier }
primop = { "add" }
prim = { identifier ~ "=" ~ primop }
file = { SOI ~ prim ~ EOI }